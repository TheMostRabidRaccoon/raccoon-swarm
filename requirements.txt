flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
python-dotenv>=1.0.0
anthropic>=0.18.0
openai>=1.0.0
google-genai>=1.0.0
python-docx>=1.0.0
PyMuPDF>=1.23.0
openpyxl>=3.1.0
requests>=2.31.0
Pillow>=10.0.0
mcp>=1.2.0

# Quantitative libraries available inside the swarm's code_exec sandbox.
# These run in subprocess.Popen([sys.executable, ...]) per swarm_codeexec.py,
# so they need to be in the same venv that runs the swarm. The swarm's
# system prompt promises models that these are preinstalled — keep this
# list in sync with that promise.
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.8
